var mongoose = require("mongoose");

var Schema = mongoose.Schema;

var scrapedArticleSchema = new Schema({
  headline: {
    type: String,
    required: true
  },
  summary: {
    type: String,
    required: true
  },
  url: {
    type: String,
    required: true
  }
});
